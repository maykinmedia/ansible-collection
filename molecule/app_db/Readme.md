# Test app_db

This is a simple test scenario where a docker container with postgres is started and used as the target.

The `app_db` role is used to provision a database user `molecule` and to create a database `molecule`.